# Context Locks

Each active Work Order receives a context lock binding its canonical basis, Git base, relevant source fingerprints and allowed scope.

A material authority/base change makes the lock `STALE` and requires recompilation before progression.
